mixin menu-item(heading, list)
  li(class='menu-item menu__primary-list-item')
    h2(class='menu-item__heading') #{heading}
    ul(class='menu-item__list')
      for item in list
        li
          a(class='menu-item__link' href='#') #{item}
